dependencies {
    compile project(':WorkcraftCore')

    // Compile all plugins
    rootDir.listFiles().findAll {
            it.isDirectory() && it =~ ".*Plugin"
        }.collect {
            compile project(":" + it.getName())
        }
}
