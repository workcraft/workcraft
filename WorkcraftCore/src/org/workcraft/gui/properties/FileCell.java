package org.workcraft.gui.properties;

import java.awt.*;

public class FileCell {

    // Clear (multiplication) symbol in UTF-8 encoding (avoid inserting UTF symbols directly in the source code).
    public static final char CLEAR_SYMBOL = 0x00D7;

    public static final Insets INSETS =  new Insets(1, 1, 1, 1);

}
